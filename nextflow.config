params {
    help = false
    genome = null
    reads = null
    annotation = null
    output = "."
}

process {
    cpus = Runtime.runtime.availableProcessors() - 2 > 0 ? Runtime.runtime.availableProcessors() - 2 : Runtime.runtime.availableProcessors()
}